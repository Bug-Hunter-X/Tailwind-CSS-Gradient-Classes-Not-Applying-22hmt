```javascript
// Some Tailwind classes aren't applied because of a missing plugin or improper configuration.
// Example:
/* This will cause an error because the plugin is not configured correctly. */
<div class="bg-gradient-to-r from-blue-500 to-purple-500">Some text</div>
```